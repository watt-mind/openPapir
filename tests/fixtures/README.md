# Synthetic fixtures

No correspondence fixtures are included in this scaffold.

Future fixtures must be created independently from invented data and covered by
[CC0](LICENSE). Record provenance or the generation method, purpose, and
expected
outcome here for every fixture. Never adapt or redact a private document into
a public fixture. Generate any test private keys at runtime; never commit keys
or
complete private-key PEM armour lines.
